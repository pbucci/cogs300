// Settings

#include "drive.c"
#include "ultra.c"

// Main loop
task main() {
	int max = maxMin();
	int range = 10;
	while(true) {
		int reading = SensorUS(ULTRA_SENSOR);
		if (fuzzy(reading,max,range)) {
			break;
		}
		turnLeft(10);
		Wait(1000);
	}
}
