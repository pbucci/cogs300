// Settings

#include "drive.c"

// Main loop
task main() {
	//	SetSensorLowspeed(ULTRA_SENSOR);
	while (true) {
		//turnLeft(10);
		//Wait(1000);
		//if (SensorUS(ULTRA) < DISTANCE_THRESHOLD) {
		//	Wait(2000);
		//}
	}
}

